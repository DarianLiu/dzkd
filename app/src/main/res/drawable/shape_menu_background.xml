<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 连框颜色值 -->
    <item>
        <shape>
            <solid android:color="@color/line_color" />
        </shape>
    </item>
    <!-- 主体背景颜色值 -->
    <item
        android:bottom="0dp"
        android:left="0dp"
        android:right="0dp"
        android:top="1px">

        <!-- 边框里面背景颜色 白色 -->
        <shape>
            <solid android:color="@color/color_menu_background" />
        </shape>
    </item>

</layer-list>
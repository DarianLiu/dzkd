<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/p46"
    android:background="@color/white"
    android:gravity="center_horizontal"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tv_tab_name"
        android:text="活动中心"
        style="@style/mine_message_center_tabhost_txt"
         />

    <ImageView
        android:id="@+id/iv_tab_red_point"
        android:layout_marginLeft="2dp"
        android:layout_toRightOf="@id/tv_tab_name"
        android:visibility="gone"
        android:layout_marginTop="@dimen/p6"
        style="@style/mine_message_center_tabhost_point"/>

</RelativeLayout>